/** Outlined submit. Fills with ink on hover -- the card's only inversion until an error. */
export function SigninButton(props: React.ButtonHTMLAttributes<HTMLButtonElement>) {
  return <button className="signin-submit" {...props} />;
}
